import {StyleSheet, Platform} from 'react-native';
import * as defaultStyle from '../../../style';

const STYLESHEET_ID = 'stylesheet.day.basic';
console.log('STYLESHEET_ID :', STYLESHEET_ID);
export default function styleConstructor(theme={}) {
  const appStyle = {...defaultStyle, ...theme};
  return StyleSheet.create({
    base: {
      width: 50,
      height: 84, // 기본 캘린더의 높이 값
      // height: appStyle.selectedDayHeight, // 기본 캘린더의 높이 값
      // height: 55, // 스케줄 목록 밑에서 나올 시 높이 값 

      // width: 32,
      // height: 32,
      alignItems: 'flex-start',
      // alignItems: 'center'
      paddingLeft: 5,
      paddingRight: 5,
      paddingBottom: 5,
    },
    text: {
      marginTop: Platform.OS === 'android' ? 4 : 6,
      fontSize: appStyle.textDayFontSize,
      fontFamily: appStyle.textDayFontFamily,
      fontWeight: '300',
      color: appStyle.dayTextColor,
      backgroundColor: 'rgba(255, 255, 255, 0)'
    },
    alignedText: {
      marginTop: Platform.OS === 'android' ? 4 : 6
    },
    selected: {
      backgroundColor: '#fff',
      // backgroundColor: appStyle.selectedDayBackgroundColor,
      borderRadius: 0,
      // borderRadius: 16
      borderTopColor: '#722784',
      borderTopWidth: 1.25,
      borderBottomColor: '#722784',
      borderBottomWidth: 1.25,
      borderLeftColor: '#722784',
      borderLeftWidth: 1.25,
      borderRightColor: '#722784',
      borderRightWidth: 1.25,
    },
    today: {
      backgroundColor: appStyle.todayBackgroundColor,
    },
    todayText: {
      color: appStyle.todayTextColor,
      fontWeight: 'bold'
    },
    selectedText: {
      color: '#722784',
      // color: appStyle.selectedDayTextColor,
    },
    disabledText: {
      color: appStyle.textDisabledColor
    },
    dot: {
      width: 8,
      height: 8,
      // width: 4,
      // height: 4,
      marginTop: 1,
      borderRadius: 5,
      // borderRadius: 2,
      opacity: 0
    },
    visibleDot: {
      opacity: 1,
      backgroundColor: appStyle.dotColor
    },
    selectedDot: {
      backgroundColor: '#722784'
      // backgroundColor: appStyle.selectedDotColor
    },
    ...(theme[STYLESHEET_ID] || {})
  });
}
